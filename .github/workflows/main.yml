name: CI/CD Deploy

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to Server via SSH
        uses: appleboy/ssh-action@master
        with:
          host: 13.48.133.135
          username: ubuntu
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e  # עצור אם יש שגיאה
            
            # 1. הגדרת נתיבים מדויקת (פעמיים Dream)
            PROJECT_ROOT="/home/ubuntu/Dream/Dream"
            CLIENT_DIR="/home/ubuntu/Dream/Dream/client"
            
            # 2. עדכון קוד
            cd $PROJECT_ROOT
            git pull origin master
            
            # 3. בנייה
            cd $CLIENT_DIR
            sudo rm -rf dist
            npm install
            npm run build
            
            # 4. העתקה לאתר (רק אם ה-build הצליח)
            if [ -d "dist" ]; then
                sudo rm -rf /var/www/html/*
                sudo cp -r dist/* /var/www/html/
                echo "Deployment successful!"
            else
                echo "Error: dist directory not found!"
                exit 1
            fi
            
            # 5. רענון
            sudo systemctl restart nginx
